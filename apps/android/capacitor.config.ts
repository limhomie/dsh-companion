import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'ai.deepseek.dsh.companion',
  appName: 'DSH Companion',
  webDir: 'www',
  backgroundColor: '#f5f6f4',
  loggingBehavior: 'debug',
  android: {
    backgroundColor: '#f5f6f4',
  },
}

export default config
